---
phase: 02-media-pipeline
plan: 01
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - backend/src/images/images.controller.ts
  - backend/src/images/images.service.ts
  - backend/src/spinner/spinner.controller.ts
  - backend/src/spinner/spinner.service.ts
  - backend/src/storage/local-storage.service.ts
  - backend/src/image-processor/image-processor.service.ts
  - frontend/src/pages/admin/ItemDetailPage.tsx
  - frontend/src/components/Spinner360/Spinner360.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - Image cover selection and ordering are deterministic after reorder mutations.
    - Spinner frame index order is contiguous and validated server-side.
    - Storage/database records stay consistent on upload and delete failures.
  artifacts:
    - backend/src/images/images.service.ts
    - backend/src/spinner/spinner.service.ts
    - backend/src/storage/local-storage.service.ts
    - frontend/src/pages/admin/ItemDetailPage.tsx
  key_links:
    - docs/DOMAIN.md
    - docs/API_CONTRACT.md
---

# Phase 2 Plan 01: Media and Spinner Reliability

## Objective
- **What:** Harden image and spinner media workflows across upload, reorder, default selection, and deletion.
- **Why:** Public and admin experiences depend on reliable media consistency.
- **Output:** Deterministic media state transitions with strong backend validation and clean UI behavior.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/DOMAIN.md
@docs/API_CONTRACT.md

## Tasks

<task type="code">
  <name>Harden regular image upload/cover/reorder semantics</name>
  <files>backend/src/images/images.service.ts, backend/src/images/images.controller.ts</files>
  <action>Guarantee one cover image policy, normalize reorder behavior, and guard against invalid image id sets in reorder payload.</action>
  <verify>Run image service unit tests and manual API checks for upload, set cover, reorder, delete.</verify>
  <done>Image ordering and cover state remain correct after any valid sequence of operations.</done>
</task>

<task type="code">
  <name>Stabilize spinner set and frame ordering logic</name>
  <files>backend/src/spinner/spinner.service.ts, backend/src/spinner/spinner.controller.ts</files>
  <action>Enforce contiguous frame_index handling, validate reorder payload integrity, and preserve single default spin set per item.</action>
  <verify>Test create set, upload frames, reorder frames, delete frame, set default scenarios.</verify>
  <done>Spinner state cannot enter inconsistent index/default conditions.</done>
</task>

<task type="code">
  <name>Add failure-safe media storage handling</name>
  <files>backend/src/storage/local-storage.service.ts, backend/src/image-processor/image-processor.service.ts, backend/src/images/images.service.ts, backend/src/spinner/spinner.service.ts</files>
  <action>Add cleanup/rollback paths when file processing or persistence fails to prevent orphaned files/records.</action>
  <verify>Simulate processing/storage failures in unit tests and verify rollback behavior.</verify>
  <done>No orphaned files or dangling DB rows remain after failure paths.</done>
</task>

<task type="code">
  <name>Align admin detail page media UX with hardened backend rules</name>
  <files>frontend/src/pages/admin/ItemDetailPage.tsx, frontend/src/components/Spinner360/Spinner360.tsx</files>
  <action>Ensure optimistic updates and mutation invalidations keep gallery/spinner order and cover/default labels in sync with backend responses.</action>
  <verify>Perform rapid upload/reorder/delete actions and confirm UI state never drifts.</verify>
  <done>Admin media operations are predictable and resilient to API latency/errors.</done>
</task>

## Verification
1. All media/spinner unit tests pass.
2. Manual test: upload 24 spinner frames, reorder, and verify correct rendered sequence.
3. Manual test: delete/reupload images while changing cover; cover always resolves to valid image.
4. No orphaned files are left after injected failure scenarios.

## Success Criteria
- [ ] Media operations are deterministic and recover safely from partial failures.
- [ ] Spinner and gallery behaviors remain consistent across backend and admin UI.

## Implementation Status (2026-03-06)
### Completed ✅
- Hardened **spinner frame upload** for deterministic `frame_index` assignment + insertion and rollback on failure.
- Hardened **image upload** for deterministic display ordering and cover selection using transactional insert.
- Added rollback cleanup for orphaned files on storage/db failure.
- Added backend reorder transaction safety for both spinner frames and regular images.
- Added a **max spinner frame limit (48)** and enforced it in the backend.

### In progress / Remaining ⚠️
- Frontend: ensure admin UI stays in sync with backend ordering rules during rapid reorder/upload/delete.
- Frontend: add automated regression tests (unit/integration) for media reorder + cover edge cases.
- Capture decisions in `02-CONTEXT.md` (currently placeholder).
