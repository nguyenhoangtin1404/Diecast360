# Phase 27-03 Plan: Production Ops Checklist + Staging Pipeline Completion

**Phase:** 27 — Auth Security Wave 2
**Plan:** 27-03
**Issues:** #245, #289
**Status:** Not started

---

## Goal

1. Document và automate checklist bật đầy đủ tính năng bảo mật trên production (Pi).
2. Hoàn thiện CI/CD staging pipeline (#289) còn dở từ Phase 25.

---

## Implementation Steps

### A. Production Ops Checklist (#245)

1. **Tạo `docs/RUNBOOKS/security-production-checklist.md`**

   ```markdown
   # Security Production Checklist (v1.0)

   ## Wave 1 — Auth Hardening (Phase 26)
   - [ ] Set AUTH_MAX_LOGIN_ATTEMPTS=5 trong Pi .env
   - [ ] Set AUTH_LOCKOUT_DURATION_MINUTES=15
   - [ ] Set AUTH_RATE_WINDOW_MINUTES=15
   - [ ] Set RECAPTCHA_SECRET_KEY (từ Google Cloud Console)
   - [ ] Set VITE_RECAPTCHA_SITE_KEY trong Vercel frontend env
   - [ ] Chạy migration: prisma migrate deploy (login_rate_limit, login_audit_log tables)
   - [ ] Verify: login sai 5 lần → bị khóa → Telegram alert gửi

   ## Wave 2 — Alerting (Phase 27)
   - [ ] Tạo Telegram Bot (BotFather) → lấy BOT_TOKEN
   - [ ] Tạo group/channel → lấy CHAT_ID
   - [ ] Set TELEGRAM_BOT_TOKEN và TELEGRAM_CHAT_ID trong Pi .env
   - [ ] Set SECURITY_ALERT_DEDUPE_TTL_MINUTES=5
   - [ ] Test: trigger lockout → verify Telegram message nhận được

   ## Verification
   - [ ] POST /api/v1/auth/login trả về X-Trace-Id header
   - [ ] Admin /admin/security/audit-log load được
   - [ ] Rate limit state survive Pi restart (check login_rate_limit table)
   ```

2. **Cập nhật `docs/ENV.md`** với tất cả ENV vars mới từ Phase 26–27

3. **Cập nhật `backend/.env.production.example`** với các vars mới (values trống, comments giải thích)

### B. Staging Pipeline Completion (#289)

1. **Review `deploy-staging.yml`** từ Phase 25:
   - Confirm trigger: `push` to `staging` branch + `workflow_run` sau CI
   - Add `concurrency` group để prevent parallel deploys

2. **Branch protection cho `staging`**:
   - Required checks: `CI Success`
   - Không cần PR để merge (direct push cho staging)

3. **Staging ENV trên Vercel**:
   - `BACKEND_URL` → staging backend URL
   - `VITE_RECAPTCHA_SITE_KEY` → staging reCAPTCHA key (dùng key test của Google)
   - Document trong `docs/DEPLOYMENT.md` section "Staging"

4. **Health check staging**:
   - Sau deploy: `curl https://staging-backend/api/v1/health` trong workflow
   - Fail deploy nếu health check fail

---

## Definition of Done

- [ ] `docs/RUNBOOKS/security-production-checklist.md` tồn tại và đầy đủ
- [ ] `docs/ENV.md` có tất cả vars từ Phase 26–27
- [ ] `backend/.env.production.example` updated
- [ ] Staging pipeline chạy ổn định: trigger → migrate → deploy → health check
- [ ] `docs/DEPLOYMENT.md` có section hướng dẫn staging
