---
phase: 07-issue-57-quantity-and-custom-attributes
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - backend/src/items/dto/create-item.dto.ts
  - backend/src/items/dto/update-item.dto.ts
  - backend/src/items/items.service.ts
  - backend/src/items/items.controller.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - API contract includes quantity and attributes with validation.
    - Invalid payloads are rejected consistently.
  artifacts:
    - backend/src/items/items.service.ts
  key_links:
    - .planning/REQUIREMENTS.md
---

# Phase 7 Plan 02: API and Validation for Product Extensions

## Objective
- **What:** Add quantity/attributes support to item APIs.
- **Why:** Backend contract must enforce data integrity.
- **Output:** Updated DTOs/services/controllers with validation coverage.

## Context
@.planning/issues/57-quantity-and-custom-attributes.md

## Tasks

<task type="code">
  <name>Update item DTOs and controller contract</name>
  <files>backend/src/items/dto/create-item.dto.ts, backend/src/items/dto/update-item.dto.ts, backend/src/items/items.controller.ts</files>
  <action>Add schema-level validation for quantity and attributes payload structure.</action>
  <verify>Controller/unit tests for valid and invalid payload cases.</verify>
  <done>Item endpoints accept and validate new fields correctly.</done>
</task>

<task type="code">
  <name>Implement service mapping and persistence rules</name>
  <files>backend/src/items/items.service.ts</files>
  <action>Persist and return quantity/attributes in create/update/list/detail responses.</action>
  <verify>Service tests confirm round-trip correctness for new fields.</verify>
  <done>API responses consistently include new fields.</done>
</task>

## Verification
1. Invalid quantity (<0) and malformed attributes are rejected.
2. Valid quantity/attributes are persisted and returned.

## Success Criteria
- [ ] API support for ATTR-01/ATTR-02 is complete.
- [ ] Validation behavior is deterministic.
