---
phase: 09-issue-13-pre-order-management
plan: 01
type: execute
wave: 1
depends_on:
  - 07-02
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations
  - backend/src/preorders/domain
autonomous: true
user_setup: []
must_haves:
  truths:
    - Pre-order schema supports lifecycle and monetary fields.
    - State model is explicit and enforceable.
  artifacts:
    - backend/prisma/schema.prisma
  key_links:
    - .planning/REQUIREMENTS.md
---

# Phase 9 Plan 01: Pre-Order Schema and State Model

## Objective
- **What:** Define persistent pre-order model and status state machine.
- **Why:** API/UI cannot be reliable without explicit lifecycle semantics.
- **Output:** Schema + transitions foundation.

## Context
@.planning/issues/13-preorder-management.md

## Tasks

<task type="code">
  <name>Create pre-order schema and relationships</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations</files>
  <action>Add pre-order entity with references, quantity, deposit, expected date, and status fields.</action>
  <verify>Migration and relation constraints validate with sample data.</verify>
  <done>Pre-order persistence model is production-safe.</done>
</task>

<task type="code">
  <name>Define lifecycle state transitions</name>
  <files>backend/src/preorders/domain</files>
  <action>Establish allowed transitions and guardrails for each pre-order status.</action>
  <verify>State transition tests for valid and invalid paths.</verify>
  <done>Lifecycle rules are explicit and testable.</done>
</task>

## Verification
1. Schema supports full lifecycle fields.
2. State transitions are deterministic.

## Success Criteria
- [ ] Pre-order data model is complete.
- [ ] State machine foundation is validated.
