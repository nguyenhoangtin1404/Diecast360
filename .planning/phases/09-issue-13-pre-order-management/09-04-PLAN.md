---
phase: 09-issue-13-pre-order-management
plan: 04
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - frontend/src/pages/PublicPreOrdersPage.tsx
  - frontend/src/pages/admin/preorders/PreOrderManagementPage.tsx
  - frontend/src/pages/admin/preorders/status.ts
  - frontend/tests/e2e/preorders.spec.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Public pre-order listing is accessible without authenticated user context.
    - Admin pre-order management has explicit campaign selection and functional participant actions.
    - Invalid lifecycle transitions are regression-covered in E2E.
    - Frontend transition map is aligned with backend domain rules.
  artifacts:
    - frontend/src/pages/PublicPreOrdersPage.tsx
    - frontend/src/pages/admin/preorders/PreOrderManagementPage.tsx
    - frontend/src/pages/admin/preorders/status.ts
    - frontend/tests/e2e/preorders.spec.ts
  key_links:
    - .planning/phases/09-issue-13-pre-order-management/09-03-SUMMARY.md
---

# Phase 9 Plan 04: Close Pre-Order MVP Review Gaps

## Objective
- **What:** Close all follow-up gaps found in phase-9 review round 2.
- **Why:** Current MVP works but cannot be considered phase-complete until public access and flow parity gaps are fixed.
- **Output:** Public-access fix, admin campaign UX wiring, transition-parity fix, and stronger E2E regression checks.

## Context
@.planning/phases/09-issue-13-pre-order-management/09-03-SUMMARY.md
@.planning/issues/13-preorder-management.md

## Tasks

<task type="code">
  <name>Make public pre-order listing resolvable without login</name>
  <files>frontend/src/pages/PublicPreOrdersPage.tsx</files>
  <action>Introduce deterministic shop resolution for `/preorders` without relying on authenticated user state (route/query/config fallback strategy).</action>
  <verify>Anonymous visitor can load pre-order cards when a valid shop context is present.</verify>
  <done>`/preorders` works for non-authenticated users in MVP flow.</done>
</task>

<task type="code">
  <name>Wire admin campaign management interactions</name>
  <files>frontend/src/pages/admin/preorders/PreOrderManagementPage.tsx</files>
  <action>Replace implicit first-item behavior with explicit campaign selector and wire participant actions to concrete UI states/handlers.</action>
  <verify>Switching campaign updates participant list; action controls are functional (not placeholders).</verify>
  <done>Admin management page is operational for multi-campaign flow.</done>
</task>

<task type="code">
  <name>Align lifecycle transition map with backend domain</name>
  <files>frontend/src/pages/admin/preorders/status.ts</files>
  <action>Sync frontend transition definitions with backend transition matrix (including ARRIVED -> CANCELLED if still valid).</action>
  <verify>Available transition actions mirror backend-allowed transitions only.</verify>
  <done>UI transition affordances are domain-consistent.</done>
</task>

<task type="test">
  <name>Add invalid-transition regression checks in E2E</name>
  <files>frontend/tests/e2e/preorders.spec.ts</files>
  <action>Add explicit invalid transition test path and assert user-facing error/guard behavior; keep happy-path and public smoke checks stable.</action>
  <verify>Playwright preorders spec passes with both happy-path and invalid-transition assertions.</verify>
  <done>Phase-9 risk areas are regression-protected.</done>
</task>

## Verification
1. Anonymous access path for public pre-order list works.
2. Campaign selector and participant actions behave correctly.
3. UI transition actions match backend rule matrix.
4. E2E includes invalid-transition coverage and remains stable.

## Success Criteria
- [ ] Public `/preorders` works without requiring authenticated user context.
- [ ] Admin campaign management flow is interactive and not placeholder-only.
- [ ] Transition options are parity-aligned with backend domain.
- [ ] Invalid transition behavior is covered in Playwright regression tests.

