---
phase: 11-issue-48-membership-and-points
plan: 01
type: execute
wave: 1
depends_on:
  - 07-02
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations
  - backend/src/members/rules
autonomous: true
user_setup: []
must_haves:
  truths:
    - Membership/points schema supports ledger-first accounting.
    - Tier logic rules are explicit and testable.
  artifacts:
    - backend/prisma/schema.prisma
  key_links:
    - .planning/REQUIREMENTS.md
---

# Phase 11 Plan 01: Membership and Points Core Model

## Objective
- **What:** Build schema and core rules for membership and points.
- **Why:** Loyalty features require audit-safe accounting foundation.
- **Output:** Ledger-based model and tier rule module.

## Context
@.planning/issues/48-membership-and-points.md

## Tasks

<task type="code">
  <name>Define membership and points ledger schema</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations</files>
  <action>Create member, tier, and points-ledger entities with required relations and constraints.</action>
  <verify>Migration applies and constraints hold on sample writes.</verify>
  <done>Core data model for membership/points is available.</done>
</task>

<task type="code">
  <name>Implement tier and points rule core</name>
  <files>backend/src/members/rules</files>
  <action>Implement deterministic earn/redeem/adjust and tier-evaluation rule helpers.</action>
  <verify>Rule unit tests cover boundary and downgrade/upgrade cases.</verify>
  <done>Rule engine behavior is predictable and tested.</done>
</task>

## Verification
1. Ledger schema supports all point mutation types.
2. Rule engine passes boundary tests.

## Success Criteria
- [ ] Membership core model is complete.
- [ ] Points/tier rules are deterministic.
