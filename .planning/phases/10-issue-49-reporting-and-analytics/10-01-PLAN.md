---
phase: 10-issue-49-reporting-and-analytics
plan: 01
type: execute
wave: 1
depends_on:
  - 08-02
  - 09-02
files_modified:
  - backend/src/reports/reports.service.ts
  - backend/src/reports/reports.controller.ts
  - backend/src/reports/reports.service.spec.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - KPI API is derived from transaction/pre-order sources.
    - Aggregation logic is deterministic across time ranges.
  artifacts:
    - backend/src/reports/reports.service.ts
  key_links:
    - .planning/REQUIREMENTS.md
---

# Phase 10 Plan 01: KPI Aggregation API

## Objective
- **What:** Build KPI aggregation backend for operations dashboard.
- **Why:** Reporting correctness starts from trustworthy backend metrics.
- **Output:** Time-range KPI APIs with tested calculations.

## Context
@.planning/issues/49-reporting-and-analytics.md

## Tasks

<task type="code">
  <name>Implement KPI aggregation service</name>
  <files>backend/src/reports/reports.service.ts, backend/src/reports/reports.service.spec.ts</files>
  <action>Aggregate stock, movement, pre-order, and posting metrics with configurable time windows.</action>
  <verify>Fixture-based unit tests validate numeric outputs.</verify>
  <done>KPI calculations are stable and test-verified.</done>
</task>

<task type="code">
  <name>Expose reporting API contract</name>
  <files>backend/src/reports/reports.controller.ts</files>
  <action>Provide endpoints for summary cards and trend series with validation.</action>
  <verify>Controller tests for filter ranges and response shape.</verify>
  <done>Reporting data is consumable by dashboard UI.</done>
</task>

## Verification
1. API results match expected fixtures.
2. Filter boundaries produce correct data windows.

## Success Criteria
- [x] KPI API is correct and stable.
- [x] Dashboard data dependencies are satisfied.
