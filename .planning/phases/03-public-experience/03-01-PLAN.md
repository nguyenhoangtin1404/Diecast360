---
phase: 03-public-experience
plan: 01
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - backend/src/public/public.controller.ts
  - backend/src/public/public.service.ts
  - backend/src/public/dto/query-public-items.dto.ts
  - frontend/src/pages/PublicCatalogPage.tsx
  - frontend/src/pages/PublicItemDetailPage.tsx
  - frontend/src/components/catalog/ItemCard.tsx
  - frontend/src/components/catalog/CatalogFilters.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - Public APIs expose only published, non-deleted items.
    - Public detail always resolves media fallback logic correctly.
    - Catalog filtering/sorting/pagination remain stable under real traffic patterns.
  artifacts:
    - backend/src/public/public.service.ts
    - backend/src/public/public.controller.ts
    - frontend/src/pages/PublicCatalogPage.tsx
    - frontend/src/pages/PublicItemDetailPage.tsx
  key_links:
    - docs/API_CONTRACT.md
    - docs/DOMAIN.md
---

# Phase 3 Plan 01: Public Catalog Consistency

## Objective
- **What:** Tighten public listing/detail contract and deliver consistent catalog UX with reliable spinner/gallery fallback.
- **Why:** Public pages are customer-facing and directly affect conversion.
- **Output:** Stable public catalog with predictable filters, item detail behavior, and clean API responses.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/API_CONTRACT.md
@docs/DOMAIN.md

## Tasks

<task type="code">
  <name>Enforce publish visibility and response consistency in public service</name>
  <files>backend/src/public/public.service.ts, backend/src/public/public.controller.ts, backend/src/public/dto/query-public-items.dto.ts</files>
  <action>Guarantee only public + non-deleted items are returned, normalize filter/sort input handling, and keep response envelope aligned with contract.</action>
  <verify>Run public module tests and validate list/detail endpoint payloads for hidden/deleted items.</verify>
  <done>Public API never leaks non-public inventory and returns stable payload shape.</done>
</task>

<task type="code">
  <name>Stabilize catalog page query state and filter UX</name>
  <files>frontend/src/pages/PublicCatalogPage.tsx, frontend/src/components/catalog/CatalogFilters.tsx, frontend/src/components/catalog/ItemCard.tsx</files>
  <action>Unify query param composition, loading/error/empty states, and card rendering for mixed inventory/media conditions.</action>
  <verify>Test combined filters, sort changes, and pagination transitions in browser.</verify>
  <done>Catalog interactions produce deterministic URL/query behavior and correct product cards.</done>
</task>

<task type="code">
  <name>Finalize item detail media fallback logic</name>
  <files>frontend/src/pages/PublicItemDetailPage.tsx, frontend/src/components/Spinner360/Spinner360.tsx</files>
  <action>Handle spinner-available vs image-only paths explicitly, including empty-media fallback and defensive rendering for partial payloads.</action>
  <verify>Open detail pages with full spinner, image-only, and missing-media cases.</verify>
  <done>Detail page always renders a sensible media experience with no runtime errors.</done>
</task>

<task type="test">
  <name>Add regression coverage for public visibility rules</name>
  <files>backend/src/public/public.service.spec.ts</files>
  <action>Add tests for visibility filtering, paging boundaries, and detail access constraints.</action>
  <verify>Run backend test suite for public module.</verify>
  <done>Critical visibility and fallback behaviors are protected by tests.</done>
</task>

## Verification
1. Public list/detail endpoints return contract-compliant payloads.
2. Hidden or soft-deleted items cannot be accessed from public routes.
3. Catalog filters and detail views work consistently for spinner and non-spinner items.
4. Public module tests cover visibility, pagination, and fallback edge cases.

## Success Criteria
- [ ] Public catalog behavior is consistent and conversion-ready.
- [ ] Visibility and media fallback rules are test-backed and stable.
