---
phase: 17-cloudflare-r2-upload-migrate-backend-media-from-local-disk-to-object-storage
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - docs/ENV.md
  - docs/DEPLOYMENT.md
  - docs/BACKEND_PI_CLOUDFLARE.md
  - docs/plans/cloudflare-r2-upload-migration.md
  - backend/scripts/README.md
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
must_haves:
  truths:
    - "ENV.md lists STORAGE_DRIVER and all R2 variables with safe example values"
    - "Deployment / Pi docs explain when `uploads/` directory is optional"
    - "One-time migration path documented (rclone or Node script outline) without committing secrets"
  artifacts:
    - path: docs/ENV.md
      provides: Operator reference for R2
    - path: docs/DEPLOYMENT.md
      provides: Cutover checklist
---

<objective>
**What:** Operator-facing documentation and optional **migration helper** outline for copying existing `UPLOAD_DIR` objects into R2 with identical keys.

**Why:** Production Pi and Docker hosts may already hold gigabytes under `./uploads`; cutover requires runbook clarity.

**Output:** Updated docs; optional `backend/scripts/sync-uploads-to-r2.ts` stub or `docs/plans/r2-cutover-runbook.md` section — pick one concrete artifact in SUMMARY.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/plans/cloudflare-r2-upload-migration.md
@docs/BACKEND_PI_CLOUDFLARE.md
@docs/ENV.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>ENV + deployment runbook</name>
  <files>docs/ENV.md, docs/DEPLOYMENT.md, docs/BACKEND_PI_CLOUDFLARE.md</files>
  <action>Add subsection **Object storage (Cloudflare R2)** in ENV: variables from 17-01, security notes (rotate keys, no public bucket listing). In DEPLOYMENT: ordered cutover — enable R2 driver on staging, sync objects, verify spot-check URLs, switch production, rollback = STORAGE_DRIVER=local + restored disk. Update Pi doc: `mkdir uploads` only when using local driver.</action>
  <verify>
    <automated>grep -n "STORAGE_DRIVER" /workspace/docs/ENV.md && grep -n "R2_" /workspace/docs/ENV.md | head -20</automated>
  </verify>
  <done>Both grep checks succeed (non-zero lines).</done>
</task>

<task type="auto">
  <name>Migration path documentation</name>
  <files>docs/plans/cloudflare-r2-upload-migration.md</files>
  <action>Append **Cutover runbook** section: prerequisites, `rclone sync` example command with R2 endpoint placeholder, post-sync validation queries (count files vs DB distinct paths optional), feature-flag order (`STORAGE_DRIVER` flip). Link to plan files 17-01..17-03 for implementers.</action>
  <verify>
    <automated>grep -n "Cutover runbook" /workspace/docs/plans/cloudflare-r2-upload-migration.md</automated>
  </verify>
  <done>Runbook heading exists.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Staging bucket smoke</name>
  <files></files>
  <action>Human: create dev bucket + token; run backend with `STORAGE_DRIVER=r2`; upload branding + one spinner frame; confirm DB paths unchanged and images render.</action>
  <verify>
    <automated>MISSING — record staging smoke outcome in 17-03-SUMMARY.md (human)</automated>
  </verify>
  <done>Checkbox recorded in 17-03-SUMMARY.md with date or blocker note.</done>
</task>

</tasks>

<verification>
Docs-only change set reviewed in PR; no production secret committed.
</verification>

<success_criteria>
- New operator can cut over without reading source code
- AGENTS.md or root dev docs optionally cross-link one line to ENV R2 section (only if truly helpful; skip if redundant)
</success_criteria>

<output>
After completion, create `17-03-SUMMARY.md`
</output>
