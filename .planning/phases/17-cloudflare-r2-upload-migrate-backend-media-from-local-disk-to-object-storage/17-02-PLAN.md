---
phase: 17-cloudflare-r2-upload-migrate-backend-media-from-local-disk-to-object-storage
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - backend/src/common/media/media.controller.ts
  - backend/src/common/media/media.controller.spec.ts
  - docs/API_CONTRACT.md
autonomous: true
requirements:
  - MEDI-01
  - MEDI-02
  - MEDI-03
must_haves:
  truths:
    - "When STORAGE_DRIVER=r2, GET /api/v1/media either streams from R2 for valid signed links OR returns a documented deprecation response; no disk read of UPLOAD_DIR for object keys"
    - "When STORAGE_DRIVER=local, MediaController behavior unchanged"
    - "Contract/docs describe how clients obtain media URLs under each driver"
  artifacts:
    - path: backend/src/common/media/media.controller.ts
      provides: R2-aware or presigned-only media path
    - path: docs/API_CONTRACT.md
      provides: Media URL semantics per driver
  key_links:
    - from: signed payload `p`
      to: S3 GetObject key
---

<objective>
**What:** Resolve `MediaController` behavior when primary delivery is **presigned R2** (from plan 17-01): either implement **proxy mode** (verify signature → `GetObject` stream from R2) for existing `/api/v1/media?...` links, or explicitly **404/410** with logging if product chooses presigned-only and confirms DB never stores API media URLs.

**Why:** Ops and cached clients may still hit `/media` for older signed URLs during rollout.

**Output:** Deterministic behavior per `STORAGE_DRIVER`, updated `media.controller.spec.ts`, and `docs/API_CONTRACT.md` section for media access.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-cloudflare-r2-upload-migrate-backend-media-from-local-disk-to-object-storage/17-RESEARCH.md
@backend/src/common/media/media.controller.ts
@backend/src/common/media/media.controller.spec.ts
@backend/src/storage/storage.interface.ts

**Default implementation choice (executor):** Implement **R2 proxy** in `MediaController` when `STORAGE_DRIVER=r2`: after signature verification, `GetObject` from bucket using payload `p` as key, pipe body to Express `res`, map Content-Type from key extension (reuse same mapping as today). Inject or lazy-build S3 client shared pattern with `R2StorageService` (extract small `R2ClientFactory` if needed to avoid duplication).

If presigned-only is preferred instead, document in SUMMARY and add feature flag `MEDIA_PROXY_ENABLED=false` behavior — but then add automated test proving old signed URL path returns documented status.
</context>

<tasks>

<task type="auto">
  <name>MediaController R2 branch + specs</name>
  <files>backend/src/common/media/media.controller.ts, backend/src/common/media/media.controller.spec.ts</files>
  <action>Read `STORAGE_DRIVER` + R2 env from `ConfigService`. For `r2`, replace `createReadStream(absolute)` path with S3 streaming (handle 404 from R2 as 404 to client). Preserve mime + Cache-Control behavior where applicable (`private` remains appropriate). Extend specs: mock S3 `send` to return readable body for happy path; assert 404 when object missing.</action>
  <verify>
    <automated>cd /workspace/backend && npx jest src/common/media/media.controller.spec.ts -x</automated>
  </verify>
  <done>Specs pass for both local and r2 branches (use per-test env or module override pattern consistent with existing spec file).</done>
</task>

<task type="auto">
  <name>API contract documentation</name>
  <files>docs/API_CONTRACT.md</files>
  <action>Document: item/spinner/branding responses return presigned R2 URLs when `STORAGE_DRIVER=r2`; optional `GET /api/v1/media` still validates `d`/`s` and may proxy from R2 during transition; local driver unchanged.</action>
  <verify>
    <automated>grep -n "media" /workspace/docs/API_CONTRACT.md | head -30</automated>
  </verify>
  <done>Contract mentions driver flag and URL expiry expectations.</done>
</task>

</tasks>

<verification>
Manual smoke (document in SUMMARY): start backend with `STORAGE_DRIVER=r2` + test bucket, upload one frame, open returned image URL in browser.
</verification>

<success_criteria>
- No accidental disk reads for R2 mode
- Automated media controller tests green
</success_criteria>

<output>
After completion, create `17-02-SUMMARY.md`
</output>
