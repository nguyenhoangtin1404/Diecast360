---
phase: 17-cloudflare-r2-upload-migrate-backend-media-from-local-disk-to-object-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/storage/r2-storage.service.ts
  - backend/src/storage/r2-storage.service.spec.ts
  - backend/src/storage/storage.module.ts
  - backend/.env.example
autonomous: true
requirements:
  - MEDI-01
  - MEDI-02
  - PLAT-02
must_haves:
  truths:
    - "With STORAGE_DRIVER=local, behavior matches pre-change LocalStorageService wiring"
    - "With STORAGE_DRIVER=r2 and valid env, saveFile/deleteFile/moveFile invoke S3 API with keys matching existing relative paths (e.g. images/foo.jpg)"
    - "Unit tests cover R2 service with mocked S3 client (no network in CI)"
  artifacts:
    - path: backend/src/storage/r2-storage.service.ts
      provides: R2-backed IStorageService
    - path: backend/src/storage/storage.module.ts
      provides: Conditional provider for IStorageService
  key_links:
    - from: IStorageService
      to: LocalStorageService | R2StorageService
---

<objective>
**What:** Add `@aws-sdk/client-s3` (+ `@aws-sdk/s3-request-presigner` if presigning is implemented in this same plan for `getFileUrl`) and a new `R2StorageService` that fulfills `IStorageService`, registered from `StorageModule` based on `STORAGE_DRIVER`.

**Why:** All upload flows already depend on `IStorageService`; swapping the provider is the smallest blast-radius path to R2.

**Output:** Runnable backend with `STORAGE_DRIVER=local` default; `r2` path covered by unit tests and documented env keys in `backend/.env.example`.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-cloudflare-r2-upload-migrate-backend-media-from-local-disk-to-object-storage/17-RESEARCH.md
@backend/src/storage/storage.interface.ts
@backend/src/storage/local-storage.service.ts
@backend/src/storage/storage.module.ts
@docs/plans/cloudflare-r2-upload-migration.md

<interfaces>
`IStorageService.saveFile(buffer, filename, subfolder?)` returns relative path string used in Prisma (`file_path`, etc.). Keys must stay stable: `${subfolder}/${filename}` with forward slashes.
`moveFile(currentPath, newFilename, destSubfolder)` must return new relative path; implement via CopyObject + DeleteObject on R2.
`getFileUrl` for this wave: implement **presigned GET** to R2 (recommended in RESEARCH) using same TTL source as local signing (`MEDIA_URL_TTL_MS` / config) so plan 17-02 can focus on MediaController edge cases only if needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Dependencies + R2StorageService skeleton</name>
  <files>backend/package.json, backend/src/storage/r2-storage.service.ts</files>
  <action>Add `@aws-sdk/client-s3` and `@aws-sdk/s3-request-presigner`. Implement `R2StorageService` with constructor injecting `ConfigService`, building S3 `Client` from `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, endpoint URL per RESEARCH. Implement `saveFile` (PutObject), `deleteFile` (DeleteObject best-effort), `moveFile` (CopyObject sourceKey=currentPath, then DeleteObject on success). Implement `getFileUrl` using presigned GetObject with expiry aligned to `MEDIA_URL_TTL_MS` (same default as local 7d if unset). Set ContentType on PutObject for common image extensions.</action>
  <verify>
    <automated>cd /workspace/backend && npx jest src/storage/r2-storage.service.spec.ts -x</automated>
  </verify>
  <done>New service file compiles; S3 client configured from env names documented in .env.example additions.</done>
</task>

<task type="auto">
  <name>Unit tests with mocked S3 client</name>
  <files>backend/src/storage/r2-storage.service.spec.ts</files>
  <action>Follow `local-storage.service.spec.ts` style: mock `@aws-sdk/client-s3` `send` to assert PutObject/CopyObject/DeleteObject input shapes; test `saveFile` returns `spinner/frame.jpg` style paths; test `moveFile` returns destination relative path; test `getFileUrl` returns string containing expected host or presigned query parameters (snapshot or substring). Cover delete swallowing NotFound if desired to match local behavior.</action>
  <verify>
    <automated>cd /workspace/backend && npx jest src/storage/r2-storage.service.spec.ts -x</automated>
  </verify>
  <done>All tests in r2-storage.service.spec.ts pass.</done>
</task>

<task type="auto">
  <name>StorageModule provider switch</name>
  <files>backend/src/storage/storage.module.ts, backend/.env.example</files>
  <action>Use `ConfigService` + factory: when `STORAGE_DRIVER=== 'r2'`, provide `R2StorageService` as `IStorageService`; else `LocalStorageService`. Fail fast at module init or first use if `r2` chosen but required R2 env vars missing (clear error message). Export same tokens as today. Append R2 variables and `STORAGE_DRIVER` to `backend/.env.example` with comments (no secrets).</action>
  <verify>
    <automated>cd /workspace && pnpm --filter ./backend test -- --testPathPattern=storage</automated>
  </verify>
  <done>Default driver unchanged; switching env to r2 in dev is possible when credentials exist.</done>
</task>

</tasks>

<verification>
Run full `pnpm --filter ./backend test` or targeted storage + one consumer spec (`spinner.service.spec`) to ensure DI still resolves `IStorageService`.
</verification>

<success_criteria>
- R2 implementation behind interface with presigned URLs
- Local remains default; CI green without R2 secrets
</success_criteria>

<output>
After completion, create `.planning/phases/17-cloudflare-r2-upload-migrate-backend-media-from-local-disk-to-object-storage/17-01-SUMMARY.md`
</output>
