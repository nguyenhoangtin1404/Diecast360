# Plan 14-01: Multi-Tenant Schema and Data Isolation

## Mục tiêu
Thiết lập nền móng Database cho Multi-Tenant.

## Tasks thực hiện
- [x] Sửa `schema.prisma`: Thêm model `Shop` và `UserShopRole`.
- [x] Thêm `ShopRole` enum (`super_admin`, `shop_admin`).
- [x] Bổ sung `shop_id` vào CUID/UUID (nullable) vào bảng `Item`.
- [x] Tạo Data Migration 3 bước an toàn:
  1. Dựng bảng cơ sở `shops`, `user_shop_roles`.
  2. Tạo 1 Default Shop (ví dụ `shop-diecast360`) cho toàn bộ items/users vũ trụ cũ.
  3. Gán tất cả Admin cũ thành `super_admin` hoặc `shop_admin` của Default Shop này.
- [x] Cập nhật tài liệu thiết kế (DB_SCHEMA.md).
