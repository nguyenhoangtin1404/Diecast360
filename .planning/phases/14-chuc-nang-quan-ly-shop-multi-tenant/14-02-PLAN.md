# Plan 14-02: API Scoping and Guard Enforcement

## Mục tiêu
Cách ly dữ liệu API và bảo vệ Endpoint Multi-Tenant.

## Tasks thực hiện
- [x] Tạo `POST /auth/switch-shop` trong `AuthService` thay đổi JWT (`active_shop_id`) vào Cookie mới.
- [x] Sửa `JwtStrategy` decode payload thêm field `active_shop_id`.
- [x] Viết `TenantGuard` và `@CurrentTenantId()` custom Decorator đọc `req.tenantId`.
- [x] Áp dụng Isolation Security: Update `ItemsService`, `ImagesService`, `SpinnerService` bắt buộc thêm params `where: { shop_id: tenantId }` nếu Request vào Tenant scope.
- [x] Fix và Update tất cả 264+ Unit tests Backend cho các logic Isolation này.
- [x] Dựng `ShopsModule` (Admin / CRUD) chỉ cho Role `@Roles('super_admin')`.
- [x] Cập nhật tài liệu thiết kế (API_CONTRACT.md).
