# Plan 14-03: Frontend Tenant Selection and UX

## Mục tiêu
Đưa Tenant selection vào Frontend UI sử dụng Shop Context. 

## Tasks thực hiện
- [x] Tạo React Context Provider: `<ShopProvider>` gọi API `/auth/switch-shop` lấy thông tin và cấp context cho UI.
- [x] Xây dựng `<ShopSelector>` cho Top Navigation Bar.
- [x] Thiết kế UI `ShopsPage.tsx`(`/admin/shops`) dành riêng cho `super_admin`: Thêm xóa quyền, Deactivate shops, và Hiển thị Member count.
- [x] Kiểm tra lại Responsive UI Phase 6 cho layout chọn Shop và Header.
- [x] Test build Frontend (`npm run build`).
