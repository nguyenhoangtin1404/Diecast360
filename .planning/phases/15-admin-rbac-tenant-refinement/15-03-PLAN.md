---
phase: 15-admin-rbac-tenant-refinement
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - frontend/src/contexts/AuthContext.tsx
  - frontend/src/hooks/useAuth.ts
  - frontend/src/pages/admin/ShopsPage.tsx
  - frontend/src/pages/admin/shops/modals/AddMemberModal.tsx
  - frontend/src/pages/admin/shops/modals/MembersListModal.tsx
  - frontend/tests/e2e/auth.spec.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Admin UI reflects platform vs shop capabilities; shop_staff does not see destructive controls.
    - Add-member flow sends selected ShopRole to API.
  artifacts:
    - frontend/src/pages/admin/ShopsPage.tsx
  key_links:
    - .codex/get-shit-done/references/ui-brand.md
    - .planning/phases/15-admin-rbac-tenant-refinement/15-CONTEXT.md
requirements:
  - MULT-04
---

# Phase 15 Plan 03: Frontend — Capability UI, Member Role, E2E

## Objective

- **What:** Đồng bộ React admin: type user từ API, ẩn/hiện menu và nút theo `platform_role` và role trong shop hiện tại; modal thêm thành viên chọn role; cập nhật nhãn tiếng Việt.
- **Why:** Tránh UI “có nút nhưng 403”; giảm hỗ trợ cho nhầm quyền.
- **Output:** E2E hoặc integration tối thiểu cho auth + shops guard message.

## Context

@frontend/src/contexts/AuthContext.tsx  
@frontend/src/pages/admin/ShopsPage.tsx  
@frontend/src/pages/admin/shops/modals/AddMemberModal.tsx  
@frontend/tests/e2e/auth.spec.ts  

## Tasks

<task type="code">
  <name>Auth types + Shops UI capability gates</name>
  <files>frontend/src/contexts/AuthContext.tsx, frontend/src/hooks/useAuth.ts, frontend/src/pages/admin/ShopsPage.tsx, frontend/src/components/admin/ShopSelector.tsx</files>
  <action>Extend types from `/auth/me` or login response for `platform_role` and per-shop roles. Gate “Quản lý shop / Danh mục global” behind platform capability. Show explanatory banner for shop_staff when write actions hidden (reuse existing alert patterns from ShopsPage).</action>
  <verify>
    <automated>cd /workspace/frontend && npm run build</automated>
  </verify>
  <done>Production build succeeds; no TS errors on new fields.</done>
</task>

<task type="code">
  <name>Member modals + E2E</name>
  <files>frontend/src/pages/admin/shops/modals/AddMemberModal.tsx, frontend/src/pages/admin/shops/modals/MembersListModal.tsx, frontend/tests/e2e/auth.spec.ts</files>
  <action>AddMemberModal: select `shop_admin` vs `shop_staff` (default shop_admin); submit field name aligned with backend DTO. MembersListModal: display role labels including shop_staff. Extend auth E2E (or add rbac-smoke spec) to assert platform user sees Shops nav item; mock responses if CI has no multi-user DB.</action>
  <verify>
    <automated>cd /workspace/frontend && npm run test:unit</automated>
  </verify>
  <done>Vitest unit suite passes; optional follow-up: `npm run test:e2e -- tests/e2e/auth.spec.ts` when Playwright env available.</done>
</task>

## Verification

1. shop_staff user: items list visible, create item button hidden or disabled with tooltip.
2. Platform user: Shops management reachable without 403 loop.

## Success Criteria

- [ ] UI aligned with Phase 15-02 API.
- [ ] Automated test coverage for primary regression path.
