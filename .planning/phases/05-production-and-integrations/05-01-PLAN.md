---
phase: 05-production-and-integrations
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - docker-compose.yml
  - backend/.env.example
  - frontend/.env.example
  - .github/workflows/ci.yml
  - backend/src/integrations/facebook/facebook.module.ts
  - backend/src/integrations/facebook/facebook.service.ts
  - backend/src/integrations/facebook/facebook.controller.ts
  - backend/src/items/items.service.ts
  - docs/ENV.md
  - docs/API_CONTRACT.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Local docker stack boots backend, frontend, and database with predictable startup.
    - CI validates lint/test/build for both apps and fails on regressions.
    - Facebook Graph API posting is integrated behind secure config and explicit error mapping.
  artifacts:
    - docker-compose.yml
    - .github/workflows/ci.yml
    - backend/src/integrations/facebook/facebook.service.ts
    - docs/ENV.md
  key_links:
    - docs/ENV.md
    - docs/API_CONTRACT.md
    - docs/ERROR_HANDLING.md
---

# Phase 5 Plan 01: Production Hardening and Facebook Graph API

## Objective
- **What:** Finalize ops baseline (Docker + CI) and implement first-class Facebook Graph API posting integration.
- **Why:** This phase transitions the project from feature-complete to production-ready.
- **Output:** Reproducible local runtime, enforceable CI quality gates, and automated Facebook posting endpoint.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ENV.md
@docs/API_CONTRACT.md
@docs/ERROR_HANDLING.md

## Tasks

<task type="code">
  <name>Finalize Docker local stack for full-system dev</name>
  <files>docker-compose.yml, backend/.env.example, frontend/.env.example, docs/ENV.md</files>
  <action>Ensure compose runs db+backend+frontend with correct networking, health checks, volumes, and env defaults documented for onboarding.</action>
  <verify>Run docker compose up and confirm services become healthy with working app endpoints.</verify>
  <done>New developer can run complete stack from documented env and compose commands.</done>
</task>

<task type="code">
  <name>Upgrade CI to enforce tests alongside lint/build</name>
  <files>.github/workflows/ci.yml, backend/package.json, frontend/package.json</files>
  <action>Add/enable test steps and ensure CI covers lint + test + build for both workspaces with reliable dependency caching.</action>
  <verify>Run local equivalents and validate workflow logic against expected exit behavior.</verify>
  <done>CI fails for lint/test/build regressions and remains deterministic across branches.</done>
</task>

<task type="code">
  <name>Implement Facebook Graph API integration module</name>
  <files>backend/src/integrations/facebook/facebook.module.ts, backend/src/integrations/facebook/facebook.service.ts, backend/src/integrations/facebook/facebook.controller.ts, backend/src/items/items.service.ts</files>
  <action>Create integration service for page post publish calls, token-based auth config, request/response mapping, and persistent link creation in existing facebook_posts flow.</action>
  <verify>Mock Graph API in unit/integration tests and validate success + common failure mappings.</verify>
  <done>Admin can trigger publish endpoint and get saved post URL with clear failure reasons.</done>
</task>

<task type="docs">
  <name>Document integration and operational contracts</name>
  <files>docs/API_CONTRACT.md, docs/ENV.md, docs/ERROR_HANDLING.md, README.md</files>
  <action>Document new FB publish endpoints/fields, required env vars, and operational troubleshooting paths.</action>
  <verify>Cross-check docs against implemented endpoint signatures and env names.</verify>
  <done>Operations and API docs are synchronized with production integration behavior.</done>
</task>

## Verification
1. `docker compose up` brings all services online and app is accessible.
2. CI definition includes lint + test + build for backend/frontend and passes on clean branch.
3. Facebook publish endpoint succeeds with valid token and fails with normalized errors for invalid token/permission/rate-limit.
4. API/env/error docs match implemented integration contract.

## Success Criteria
- [ ] Development and CI pipelines are production-grade and repeatable.
- [ ] Facebook posting is automated through Graph API with safe error handling.
